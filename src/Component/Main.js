import React from "react";

export const Main = (props) => {
  return (
    <ul>
      <div>
        <li>{props.text}</li>
        <button>dell</button>
      </div>

      <input></input>
      <button>add</button>
    </ul>
  );
};
